import type { FC } from "react";

export const About: FC = () => (
  <div className="text-center">
    <h2 className="text-3xl font-semibold mb-4">About Us</h2>
    <p className="text-lg">
      We build awesome SPAs with React, TypeScript, and Tailwind.
    </p>
  </div>
);
